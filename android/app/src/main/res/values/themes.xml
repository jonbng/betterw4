<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Cold-start splash (UWC mark + W4 on UWC blue). Switches to Theme.BetterW4 after
      installSplashScreen() / first frame.
    -->
    <style name="Theme.BetterW4.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/android12splash</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="postSplashScreenTheme">@style/Theme.BetterW4</item>
    </style>

    <!-- AppCompat parent required by MainActivity (AppCompatActivity) for per-app locales. -->
    <style name="Theme.BetterW4" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:colorBackground">@color/splash_background</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
